import { neo4jgraphql } from "neo4j-graphql-js";

const resolvers = {
//   Query: {
//     FlighsSearch(object, params, ctx, resolveInfo) {
//       var result = neo4jgraphql(object, params, ctx, resolveInfo);
//       console.log(result);
//       return result;
//     }
//   }
};